import styled from 'styled-components/native';
import { StyledText } from '../../../components/TextBox/style';

export const StyledContainer = styled.View`
  margin: 46px 0px;
`;

export const InstructionText = styled(StyledText)`
    margin: 24px 0px;
`